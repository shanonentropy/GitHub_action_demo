name: Hello GitHub Actions

on:
  push:
    branches:
      - main  # or 'master' depending on your repo name

jobs:
  say_hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository code
        uses: actions/checkout@v4

      - name: Print a message
        run: echo "Hello from GitHub Actions!"
